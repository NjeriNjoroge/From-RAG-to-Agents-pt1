[project]
name = "from-rag-to-agents-pt1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.96.0",
    "jupyter>=1.1.1",
]
